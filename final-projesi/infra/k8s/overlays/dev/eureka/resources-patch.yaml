apiVersion: apps/v1
kind: Deployment
metadata:
  name: eureka
spec:
  template:
    spec:
      containers:
        - name: eureka
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "384Mi"
