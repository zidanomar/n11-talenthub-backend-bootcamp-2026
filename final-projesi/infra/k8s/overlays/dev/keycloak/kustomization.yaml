apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../base/keycloak/
  - keycloak-secret.yaml

configMapGenerator:
  - name: keycloak-realm
    files:
      - nilfheim-realm.json
    options:
      disableNameSuffixHash: true
