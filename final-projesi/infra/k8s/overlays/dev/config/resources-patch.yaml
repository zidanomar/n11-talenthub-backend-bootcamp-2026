apiVersion: apps/v1
kind: Deployment
metadata:
  name: config
spec:
  template:
    spec:
      containers:
        - name: config
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "384Mi"
